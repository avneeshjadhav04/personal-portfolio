# Trunk build configuration for the Leptos CSR portfolio.
# https://trunkrs.dev/configuration/

[build]
target = "index.html"
dist = "dist"
wasm_opt = false

[serve]
address = "0.0.0.0"
port = 1420
open = false
no_autoreload = false

[[copy]]
source = "public"
target = "public"